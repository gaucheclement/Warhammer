{
  "name": "warhammer-data-extractor",
  "version": "1.0.0",
  "description": "Warhammer character generator with automated data extraction from Google Apps Script",
  "main": "extract-data.js",
  "scripts": {
    "extract": "node extract-data.js",
    "extract:validate": "node extract-data.js --validate-only",
    "extract:verbose": "node extract-data.js --verbose",
    "prebuild": "npm run extract",
    "build": "echo \"Build process will be configured later - currently runs data extraction\"",
    "build:skip-extract": "echo \"Build without extraction - using cached data\""
  },
  "keywords": [
    "warhammer",
    "character-generator",
    "data-extraction",
    "google-apps-script"
  ],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^16.4.5"
  },
  "engines": {
    "node": ">=14.0.0"
  }
}
